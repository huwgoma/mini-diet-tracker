<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
  </head>

  <header>
    <h1>Mini Diet Tracker</h1>

    <% if session[:success] %>
      <div><%= session.delete(:success) %></div>
    <% end %>

    <% if session[:error] %>
      <div><%= session.delete(:error) %></div>
    <% end %>
  </header>

  <body>
    <%== yield %>
  </body>
</html>